---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This ensures the SOPS component looks in the right place
namespace: flux-system

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./romm/ks.yaml
  - ./vaultwarden/ks.yaml
  - ./storage.yaml
