apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: romm
  namespace: romm
spec:
  parentRefs:
    - name: envoy-external
      namespace: network
  hostnames:
    - "romm.kadenzobel.com"
  rules:
    - backendRefs:
        - name: romm
          port: 80
